# Runtime database — contains password hashes, sessions and the draw secret.
# Never commit this.
data/

node_modules/
*.log

# Local env secrets (DATABASE_URL etc.) and Vercel local state
.env
.vercel/
.vercel
.env*
